       "neovim/nvim-lspconfig", confing = function()
           local lspconfig = require("lspconfig")
           lspconfig.clangd.setup({})
       end,
   },
   {
       "williamboman/mason.nvim",
       config = function()
           require("mason").setup()
       end
   }:q

